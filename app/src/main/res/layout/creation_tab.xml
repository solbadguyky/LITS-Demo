<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="?attr/actionBarSize">

    <!-- Menu/Back/Action button -->
    <FrameLayout
        android:id="@+id/creationTab_actionView"
        android:layout_width="wrap_content"
        android:layout_height="?attr/actionBarSize"
        android:tag="actionView">

        <View
            android:layout_width="100dp"
            android:layout_height="?attr/actionBarSize"
            android:background="@color/md_blue_grey_50" />

    </FrameLayout>

    <!--Creation Buttons-->
    <solstudios.app.moduls.creationtab.CreationButton
        android:id="@+id/creationTab_actionFirstView"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:minWidth="100dp"
        app:layout_behavior="solstudios.app.moduls.creationtab.behaviors.CreationTabChildBehavior" />


</android.support.design.widget.CoordinatorLayout>
